# Model Files (Not Uploaded)

This folder is used for trained model weights (for example `.pth` files) during local development.

Large model artifacts are intentionally kept local and excluded from Git tracking for a lightweight repository.
